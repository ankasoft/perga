# An ignored note

This file lives under a directory named in the vault's `.gitignore`, so the
walker skips it unless `files.respect_gitignore` is turned off. It exists to
prove that both halves of that switch work.
